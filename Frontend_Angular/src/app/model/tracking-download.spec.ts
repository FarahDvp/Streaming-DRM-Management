import { TrackingDownload } from './tracking-download';

describe('TrackingDownload', () => {
  it('should create an instance', () => {
    expect(new TrackingDownload()).toBeTruthy();
  });
});
